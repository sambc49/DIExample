# DIExample
Playing around with dependency injection with .net core 3.1

Simulates repository that would interact with a db, used in a service along with a logger class which just logs to console.
DI to be introduced to allow abstraction & testability when mocking the service
